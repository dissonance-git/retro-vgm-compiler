name: GitLab exact SPC audible build

on:
  push:
    paths:
      - '.github/workflows/gitlab-spc-only-build.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: windows-2022
    timeout-minutes: 60
    env:
      GITLAB_REF: 497a0f95a2075f8eb5a55882aa76c068ab2ab694
    steps:
      - name: Clone exact GitLab VGM Compiler source
        shell: pwsh
        run: |
          git clone --filter=blob:none --no-checkout https://gitlab.com/dissonance-git/vgm-compiler.git source
          if ($LASTEXITCODE -ne 0) { throw "GitLab clone failed: $LASTEXITCODE" }
          git -C source config core.autocrlf false
          git -C source fetch origin $env:GITLAB_REF --depth=1
          if ($LASTEXITCODE -ne 0) { throw "GitLab fetch failed: $LASTEXITCODE" }
          git -C source checkout --detach $env:GITLAB_REF
          if ($LASTEXITCODE -ne 0) { throw "GitLab checkout failed: $LASTEXITCODE" }
          $actual = (git -C source rev-parse HEAD).Trim()
          if ($actual -ne $env:GITLAB_REF) { throw "SHA mismatch actual=$actual expected=$env:GITLAB_REF" }
          Push-Location source
          python -B tools/verify_build_source_provenance.py . --expected-commit $env:GITLAB_REF
          if ($LASTEXITCODE -ne 0) { throw "source provenance failed" }
          Pop-Location
          Write-Host "GITLAB_EXACT_SPC_SOURCE_OK $actual"

      - name: Ensure native build tools
        shell: pwsh
        run: |
          choco install nasm --version=2.16.3 --allow-downgrade -y --no-progress
          if ($LASTEXITCODE -ne 0) { throw "NASM install failed: $LASTEXITCODE" }
          if (-not (Get-Command 7z -ErrorAction SilentlyContinue)) {
            choco install 7zip -y --no-progress
          }
          $nasmCandidates = @(
            'C:\Program Files\NASM\nasm.exe',
            'C:\Program Files (x86)\NASM\nasm.exe',
            'C:\ProgramData\chocolatey\bin\nasm.exe'
          )
          $nasmExePath = $nasmCandidates | Where-Object { Test-Path -LiteralPath $_ } | Select-Object -First 1
          if (-not $nasmExePath -and (Test-Path 'C:\ProgramData\chocolatey\lib\nasm')) {
            $nasmExePath = Get-ChildItem 'C:\ProgramData\chocolatey\lib\nasm' -Filter nasm.exe -File -Recurse -ErrorAction SilentlyContinue | Select-Object -First 1 -ExpandProperty FullName
          }
          if (-not $nasmExePath) { throw 'nasm.exe not found' }
          (Split-Path $nasmExePath -Parent) | Out-File -FilePath $env:GITHUB_PATH -Encoding utf8 -Append
          $env:Path = "$(Split-Path $nasmExePath -Parent);$env:Path"
          & $nasmExePath -v

      - name: Run SPC source-session and materialization guards
        shell: pwsh
        working-directory: source
        run: |
          python -B -m unittest tests.spc.test_surround_omniphony_patch_contract
          if ($LASTEXITCODE -ne 0) { throw 'SPC source-session patch contract failed' }
          python -B tests/spc/test_materialize_foo_snesapu.py
          if ($LASTEXITCODE -ne 0) { throw 'SPC materialization failed' }
          python -B -m unittest tests.test_private_component_builder_contract
          if ($LASTEXITCODE -ne 0) { throw 'private builder contract failed' }

      - name: Build package and prove audible SPC
        shell: pwsh
        working-directory: source
        run: |
          $wtlInclude = Join-Path $PWD '.private-component-build\WTL\Include'
          $env:INCLUDE = "$wtlInclude;$env:INCLUDE"
          $rsp = Join-Path $PWD 'Directory.Build.rsp'
          '/p:ConformanceMode=false' | Set-Content -LiteralPath $rsp -Encoding ASCII
          $env:_CL_ = "$env:_CL_ /permissive /Zc:twoPhase-"
          try {
            ./tools/build_private_foobar_components.ps1 -SpcOnly
          } finally {
            Remove-Item -LiteralPath $rsp -Force -ErrorAction SilentlyContinue
          }

      - name: Show SPC artifact evidence
        shell: pwsh
        working-directory: source
        run: |
          Get-Content dist/private-components/SHA256SUMS-SPC.txt

      - name: Upload audible SPC component
        uses: actions/upload-artifact@v4
        with:
          name: gitlab-spc-audible-${{ github.sha }}
          path: |
            source/dist/private-components/foo_snesapu.private.fb2k-component
            source/dist/private-components/SHA256SUMS-SPC.txt
          if-no-files-found: error
          retention-days: 14
